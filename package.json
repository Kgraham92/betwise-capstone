{
  "name": "betwise",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"pnpm --filter client dev\" \"pnpm --filter @betwise/server dev\"",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter @betwise/server dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.0",
  "pnpm": {
    "overrides": {
      "react": "19.2.3",
      "react-dom": "19.2.3"
    }
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
